# FastAPI and Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Database (선택사항 - Spring Boot에서 처리하지만 필요시 사용)
# sqlalchemy==2.0.25
# psycopg[binary]==3.3.2
# alembic==1.13.1

# AI/ML Libraries
langchain>=1.0.0
langchain-openai>=0.2.0
openai>=1.54.0
transformers>=4.46.0
torch>=2.5.0
sentence-transformers>=3.0.0

# Vector Database (PostgreSQL + pgvector)
sqlalchemy>=2.0.25
psycopg[binary]>=3.2.0
pgvector>=0.3.6

# Computer Vision
opencv-python>=4.10.0
pillow>=11.0.0
# face-recognition>=1.3.0  # 선택사항: dlib 빌드 필요 (CMake 필요)
# 대안: mediapipe 또는 insightface 사용 가능
mediapipe>=0.10.0  # 더 간단한 얼굴 인식 라이브러리

# Audio Processing
openai-whisper>=20241122
librosa>=0.10.3

# Video Processing
moviepy>=1.0.3

# Data Processing
pandas>=2.2.3
numpy>=2.1.0

# Task Queue (선택사항 - 필요시 사용)
# celery==5.3.6
# redis==5.0.1

# Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.12
aiofiles>=24.1.0
httpx>=0.27.0

# Logging and Monitoring
loguru>=0.7.3

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

tensorflow
tf-keras